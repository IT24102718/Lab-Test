[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback so direct navigation/refresh works with react-router-dom
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
